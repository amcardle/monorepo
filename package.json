{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "nbuild": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "build": "npm run nbuild && npm run generate && cp -r ./.output/public ./build"
  },
  "devDependencies": {
    "nuxt": "^3.4.0"
  }
}
