{
  "name": "monorepo",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "exit 0",
    "test": "echo \"Error: no test specified\" && exit 1",
    "live-server": "live-server out"
  },
  "dependencies": {
    "live-server": "^1.2.2"
  }
}
